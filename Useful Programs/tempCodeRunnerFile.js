for (let i = 0; i < primes.length; i++) {
    //     if (primes[i]) res.push(i);
    // }
    // primes = primes.map((p, index) => [p, index]).filter(([p, index]) => p === true).map(([p, index]) => index);