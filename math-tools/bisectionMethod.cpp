#include <iostream>

using namespace std;

int main() {
    float lower, upper;
    // Eq isax2 + bx + c = 0
    int a, b, c;
    cout << "Enter a number";
    cin >> a;
    cout << a << endl;
    return 0;
}
